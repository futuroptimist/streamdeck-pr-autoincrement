name: Release Stream Deck Plugin

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'SemVer version (X.Y.Z)'
        required: true
        type: string
  push:
    tags:
      - 'v*'

concurrency:
  group: release
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    if: github.event_name != 'push' || github.actor != 'github-actions[bot]'
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: |
          set -euo pipefail
          npm ci

      - name: Resolve version
        id: version
        shell: bash
        run: |
          set -euo pipefail
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            version="${{ inputs.version }}"
            if ! [[ "$version" =~ ^(0|[1-9][0-9]*)\.(0|[1-9][0-9]*)\.(0|[1-9][0-9]*)$ ]]; then
              echo "Invalid version: $version. Expected X.Y.Z with no leading zeros." >&2
              exit 1
            fi
            git fetch --tags origin
            if git ls-remote --tags origin "refs/tags/v${version}" | grep -q .; then
              echo "Tag v${version} already exists on origin." >&2
              exit 1
            fi
            if curl --fail --silent --location \
              -H "Authorization: Bearer ${{ github.token }}" \
              -H "Accept: application/vnd.github+json" \
              "https://api.github.com/repos/${{ github.repository }}/releases/tags/v${version}" \
              >/dev/null; then
              echo "Release v${version} already exists." >&2
              exit 1
            fi
          else
            ref="${GITHUB_REF_NAME}"
            if ! [[ "$ref" =~ ^v(0|[1-9][0-9]*)\.(0|[1-9][0-9]*)\.(0|[1-9][0-9]*)$ ]]; then
              echo "Invalid tag ${ref}. Expected vX.Y.Z with no leading zeros." >&2
              exit 1
            fi
            version="${ref#v}"
            if curl --fail --silent --location \
              -H "Authorization: Bearer ${{ github.token }}" \
              -H "Accept: application/vnd.github+json" \
              "https://api.github.com/repos/${{ github.repository }}/releases/tags/${ref}" \
              >/dev/null; then
              echo "Release ${ref} already exists." >&2
              exit 1
            fi
          fi
          echo "version=${version}" >> "$GITHUB_OUTPUT"
          echo "tag=v${version}" >> "$GITHUB_OUTPUT"

      - name: Bump versions
        if: github.event_name == 'workflow_dispatch'
        run: |
          set -euo pipefail
          VERSION="${{ steps.version.outputs.version }}"
          CURRENT="$(node -p "require('./package.json').version")"
          if [ "$CURRENT" = "$VERSION" ]; then
            node scripts/update-manifest-version.mjs "${VERSION}"
          else
            npm version "${VERSION}" --no-git-tag-version
            node scripts/update-manifest-version.mjs "${VERSION}"
          fi

      - name: Build
        run: |
          set -euo pipefail
          npm run build

      - name: Validate versions match tag
        if: github.event_name == 'push'
        run: |
          set -euo pipefail
          node -e "const fs=require('fs');const tag='${{ steps.version.outputs.version }}';const pkg=JSON.parse(fs.readFileSync('package.json','utf8'));const manifest=JSON.parse(fs.readFileSync('com.futuroptimist.prlist.sdPlugin/manifest.json','utf8'));const manifestExpected=\`\${tag}.0\`;if(pkg.version!==tag){throw new Error(\`package.json version \${pkg.version} does not match tag \${tag}\`);}if(manifest.Version!==manifestExpected){throw new Error(\`manifest.json Version \${manifest.Version} does not match expected \${manifestExpected}\`);}"

      - name: Install Stream Deck CLI
        run: |
          set -euo pipefail
          npm install --global @elgato/cli

      - name: Validate plugin
        run: |
          set -euo pipefail
          streamdeck validate com.futuroptimist.prlist.sdPlugin

      - name: Validate and pack plugin
        id: pack
        run: |
          set -euo pipefail
          streamdeck pack com.futuroptimist.prlist.sdPlugin
          PACKAGE_PATH="$(find . -maxdepth 2 -name '*.streamDeckPlugin' -print -quit)"
          if [ -z "$PACKAGE_PATH" ]; then
            echo "Error: Could not locate packaged .streamDeckPlugin output." >&2
            exit 1
          fi
          echo "package_path=${PACKAGE_PATH}" >> "$GITHUB_OUTPUT"

      - name: Verify package artifact
        run: |
          set -euo pipefail
          test -s "${{ steps.pack.outputs.package_path }}"

      - name: Commit and tag
        if: github.event_name == 'workflow_dispatch'
        run: |
          set -euo pipefail
          BRANCH="${GITHUB_REF#refs/heads/}"
          if [[ -z "$BRANCH" || "$BRANCH" == "$GITHUB_REF" ]]; then
            echo "Unexpected GITHUB_REF=$GITHUB_REF (expected refs/heads/*)" >&2
            exit 1
          fi
          echo "Resolved branch: ${BRANCH}"
          VERSION="${{ steps.version.outputs.version }}"
          echo "Release version: ${VERSION}"
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add package.json package-lock.json \
            com.futuroptimist.prlist.sdPlugin/manifest.json
          COMMIT_CREATED=false
          if ! git diff --cached --quiet; then
            git commit -m "chore(release): v${VERSION}"
            COMMIT_CREATED=true
          fi
          git tag "v${VERSION}"
          if [ "$COMMIT_CREATED" = true ]; then
            git push origin "HEAD:${BRANCH}"
          fi
          git push origin "v${VERSION}"

      - name: Publish release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: "${{ steps.version.outputs.tag }}"
          name: "${{ steps.version.outputs.tag }}"
          body: |
            Generates a 4-line list of incrementing GitHub PR URLs from your clipboard.
          files: ${{ steps.pack.outputs.package_path }}
